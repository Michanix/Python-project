Python project for Computer Programming course.
Face recognition project written in Python.


<h1>Requirements:</h1>

python 3.6+
Linux or Mac(face_recognition library may not work correctly on Windows)


Install process:

If requirements are satisfie, then just run the next command:
pip install -r requirements.txt